# PRD — AI Vendor Feed (X/Twitter) Aggregator (Web 默认 72h + 日期折叠 + 官方 Embed)
> Tech: Next.js (App Router) + Neon(Postgres) + Drizzle + Vercel Cron  
> Data provider: twitterapi.io（第三方 X 数据 API）  
> Display: X 官方 Embed（widgets.js + blockquote）  
> Timezone: Asia/Tokyo（展示侧按东京时区，存储用 UTC）

---

## 1. 背景与目标

### 1.1 背景
你关注的一批大模型厂商账号（OpenAI / Anthropic / Google 等）会持续发布更新。你想要一个站点：
- 自动聚合这些账号最近 **72 小时**的推文（Web/Desktop 默认展示窗口）
- 统一按时间倒序展示
- 左侧可按 **年/月/日**折叠选择日期回看
- 每条推文以 **官方 Embed** 展示（避免自己渲染内容引发合规/展示问题）
- 支持响应式、暗/亮主题、中/英文切换
- 部署 Vercel，数据库 Neon，ORM Drizzle，定时任务 Vercel Cron

### 1.2 产品目标（MVP）
- ✅ 自动：每 24 小时拉取一次关注账号的推文（定时任务）
- ✅ 展示（Web/Desktop）：默认展示最近 72h，按日期（Year-Month-Date）分组；滚动到底自动加载更早日期；提供 Back to top
- ✅ 展示（Mobile）：默认展示最近 48h；滚动到底自动加载更早日期
- ✅ 可回看：按日期（年/月/日折叠）查看历史（至少保留 30/90 天，见保留策略）
- ✅ 可筛选：按厂商标签筛选
- ✅ 可用：移动端/桌面端良好体验
- ✅ 展示：每条推文用官方 Embed 渲染

### 1.3 非目标（先别做）
- 不做登录/订阅/多用户
- 不做全文 NLP 总结/翻译（可以后续加）
- 不做“全网 AI 新闻”关键词抓取（当前数据源是账号白名单）
- 不做绕过 X 的限制/反爬（MVP 不碰爬虫）

---

## 2. 用户与使用场景

### 用户画像
- 你自己 & 少量访客：每天快速扫厂商动态，能按日期回看

### 核心场景
1. 打开首页（Web/Desktop），默认看到 **最近 72 小时**的推文流，按日期分组展示（Year-Month-Date），最新在上
2. 下滑到页面底部，自动加载更早日期的 feed（无限滚动）
3. 点击厂商标签，只看 OpenAI / Anthropic / Google
4. 点击“回到顶部”按钮，快速回到最顶部继续浏览最新内容
5. 切换语言（EN/中文）、切换主题（Dark/Light）

---

## 3. 需求与约束

### 3.1 关键约束
- 数据获取：使用 twitterapi.io（需要该服务的 API Key）
- 展示：只用官方 embed（widgets.js + blockquote）
- 更新频率：24 小时
- 数据源：账号白名单 + 厂商映射
- 部署：Vercel
- DB：Neon(Postgres) + Drizzle

### 3.2 合规/风险说明（写进 PRD，别自欺欺人）
- 你不直接调用 X 官方 API，但你依赖第三方提供的数据能力；合规与稳定性取决于第三方的获取方式与持续可用性。
- 展示层坚持用官方 Embed，可以降低“内容再分发/展示不完整”的风险。
- 需要在站点隐私政策中说明可能加载第三方脚本与 cookie（widgets.js）。

---

## 4. 信息架构与交互

### 4.1 页面结构
- `/` 首页（Web/Desktop 默认最近 72h）
- `/?vendor=openai` 按厂商筛选
- `/?date=YYYY-MM-DD` 按日期筛选/跳转（东京时区语义）
- 可选：`/about`, `/privacy`

### 4.2 布局（响应式）
**Desktop（≥ md）**
- 左侧 Sidebar（固定/可滚动）
  - Vendor 筛选（chips / radio）
  - 日期折叠树 Year > Month > Day（折叠，用于跳转到指定日期）
- 右侧 Feed（按日期分组 + 无限滚动；滚动到底部自动加载更早日期）
- 右下角悬浮按钮：Back to top（滚动一定距离后出现）

**Mobile（< md）**
- 顶部栏：Vendor chips + 主题/语言按钮 + Filter 按钮
- Filter 使用 Drawer/BottomSheet（包含 Vendor + 日期树）
- Feed 单列展示（默认 48h；滚动到底自动加载更早日期）

### 4.3 排序与默认值
- 默认（Web/Desktop）：最近 72h + 全部厂商 + `published_at DESC`，并按日期分组展示（Year-Month-Date 分隔；分组日期按 Asia/Tokyo 语义）
- 默认（Mobile）：最近 48h + 全部厂商 + `published_at DESC`（同样按日期分组展示；滚动加载更早日期）
- 无限滚动：到页面底部后，继续加载更早日期的推文（仍按时间倒序）
- 日期页：该日 00:00–23:59（Asia/Tokyo）范围内推文，按时间倒序

### 4.4 ASCII Wireframes（基于当前 PRD）

#### Web 端（Desktop ≥ md）：Sidebar + Feed（按日期分组）+ Back to top

```text
┌──────────────────────────────────────────────────────────────────────────────┐
│ AI Vendor Feed                                                               │
│ URL: /?vendor=<slug|all>&date=YYYY-MM-DD                                     │
├───────────────────────┬──────────────────────────────────────────────────────┤
│ Sidebar (fixed/scroll)│ Feed (scroll, infinite; grouped by date)             │
│                       │                                                      │
│ Vendor                │  ┌───────────────────────────────────────────────┐   │
│ [All] [OpenAI] [...]  │  │ 2025-12-24                                    │   │
│                       │  ├───────────────────────────────────────────────┤   │
│ Jump to Date          │  │ Tweet Embed (X official)                       │   │
│  2025 ▾               │  └───────────────────────────────────────────────┘   │
│   12 ▾                │  ┌───────────────────────────────────────────────┐   │
│    24                 │  │ Tweet Embed (X official)                       │   │
│    23                 │  └───────────────────────────────────────────────┘   │
│    ...                │                                                      │
│                       │  ┌───────────────────────────────────────────────┐   │
│ Theme: [Light/Dark]   │  │ 2025-12-23                                    │   │
│ Lang:  [EN/中文]      │  ├───────────────────────────────────────────────┤   │
│                       │  │ Tweet Embed (X official)                       │   │
│                       │  └───────────────────────────────────────────────┘   │
│                       │                 ...                                 │
│                       │  ┌───────────────────────────────────────────────┐   │
│                       │  │ Loading older dates...                          │   │
│                       │  └───────────────────────────────────────────────┘   │
│                       │                                      [↑ Top]        │
└───────────────────────┴──────────────────────────────────────────────────────┘
```

#### 移动端（Mobile < md）：Top Bar + Filter Drawer/BottomSheet + Feed 单列

```text
┌───────────────────────────────────────┐
│ [Vendor chips: All OpenAI ...]  [☀/🌙] │
│ [EN/中文]                      [Filter] │
├───────────────────────────────────────┤
│ Feed (single column)                  │
│ ┌───────────────────────────────────┐ │
│ │ 2025-12-24                        │ │
│ │ Tweet Embed (X official)          │ │
│ └───────────────────────────────────┘ │
│ ┌───────────────────────────────────┐ │
│ │ Tweet Embed (X official)          │ │
│ └───────────────────────────────────┘ │
│                ...                    │
└───────────────────────────────────────┘

┌───────────────────────────────────────┐
│ BottomSheet / Drawer                  │
│ ───────────────────────────────────── │
│ Vendor                                │
│ [All] [OpenAI] [Anthropic] [...]      │
│                                       │
│ Jump to Date                          │
│  2025 ▾                               │
│   12 ▾                                │
│    24                                 │
│    23                                 │
│                                       │
│ [Apply]                       [Close] │
└───────────────────────────────────────┘
```

---

## 5. 数据源与抓取策略（twitterapi.io）

### 5.1 你需要的两个能力
> 只靠 “get_tweet_by_ids” 不够，你还必须能“发现新 tweet_id”。

1) **发现（Discovery）**：按账号获取最新推文列表（timeline / recent tweets）或按 from:handle 查询（search）
2) **详情（Hydration，可选）**：对 tweet_id 列表补充字段（created_at、lang、metrics 等）

MVP 推荐：  
- 直接用“按账号拉最新推文”接口拿到 tweet_id + created_at（最好一次就够）  
- 只有当 discovery 返回的信息不全，才用 `get_tweet_by_ids` 做 hydration

### 5.2 抓取窗口与增量
- 频率：每 24 小时
- 窗口：`now_utc - 72h` 到 `now_utc`（对齐 Web 默认展示窗口；同时降低漏抓风险）
- 增量策略：
  - 每个账号记录 `last_seen_published_at` 或 `last_seen_tweet_id`
  - 每次拉“最新 N 条”（例如 30/50），过滤掉已入库与超窗的
- 去重：tweet_id 唯一（DB 约束）

### 5.3 过滤规则（MVP）
- 默认只收录：原推（默认排除 replies & retweets）
- 语言：不强过滤（展示可按语言切换 UI 文案；推文内容仍由 embed 决定）

---

## 6. 数据模型（Neon + Drizzle）

### 6.1 表结构

#### `vendors`
| field | type | notes |
|---|---|---|
| id | uuid / serial | PK |
| slug | text unique | `openai`, `anthropic`, `google`, `meta`, `xai`, `other` |
| name_en | text | |
| name_zh | text | |
| sort_order | int | |

#### `accounts`
| field | type | notes |
|---|---|---|
| id | uuid / serial | PK |
| vendor_id | fk | -> vendors.id |
| handle | text unique | `OpenAI` |
| display_name | text | optional |
| is_active | bool | default true |
| last_seen_tweet_id | text | optional |
| last_seen_published_at | timestamptz | optional |
| created_at / updated_at | timestamptz | |

#### `tweets`
| field | type | notes |
|---|---|---|
| tweet_id | text | PK |
| account_id | fk | -> accounts.id |
| vendor_id | fk | denormalized ok |
| tweet_url | text | **统一存 `https://twitter.com/{handle}/status/{tweet_id}`**（embed 更稳） |
| published_at | timestamptz | 以 UTC 存 |
| fetched_at | timestamptz | 以 UTC 存 |
| is_reply | bool | optional |
| is_retweet | bool | optional |
| lang | text | optional |
| raw_json | jsonb | optional（排错用，MVP 可关） |

**索引建议**
- `tweets(published_at DESC)`
- `tweets(vendor_id, published_at DESC)`
- `tweets(account_id, published_at DESC)`

#### `daily_index`（加速日期折叠树）
| field | type | notes |
|---|---|---|
| date_tokyo | date | PK（按 Asia/Tokyo 计算的日期） |
| tweet_count | int | |
| updated_at | timestamptz | |

> 说明：date 树数据从 `daily_index` 来，避免前端每次扫 tweets 表做聚合。

### 6.2 数据保留策略
- MVP 建议保留：90 天（可配置）
- Cron 每天/每周做一次清理（或每次 ingest 后顺便清理超期数据）

---

## 7. 后端 API 设计（Next.js Route Handlers）

### 7.1 公共接口

#### `GET /api/feed`
Query:
- `mode=timeline|date`（默认 timeline）
- `windowHours`（仅 mode=timeline；默认 72。Mobile 传 48）
- `date=YYYY-MM-DD`（mode=date 必填；东京时区语义）
- `vendor=all|openai|anthropic|google|...`
- `cursor`（可选，基于 published_at + tweet_id 组合游标）
- `limit`（默认 10，最大 30）

Return:
```json
{
  "items": [
    {
      "tweet_id": "123",
      "tweet_url": "https://twitter.com/OpenAI/status/123",
      "vendor": "openai",
      "published_at": "2025-12-24T01:23:45Z"
    }
  ],
  "nextCursor": "..."
}
